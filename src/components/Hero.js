const Hero = ({superhero, publisher, alter_ego, first_appearance}) => {
  return(
    <div className='col col-3 text-center border border-dark p-1 g-2 bg-warning'>
      WHAT????
    </div>

  )
}

export default Hero;
